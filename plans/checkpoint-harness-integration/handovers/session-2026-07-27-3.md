---
type: planning
entity: session-handover
plan: "checkpoint-harness-integration"
session_date: "2026-07-27"
phase: 4
---

# Session Handover: 2026-07-27 (Session 3)

> [checkpoint-harness-integration](../plan.md) - [Phase 4](../phases/phase-4.md)

## Session Summary

Resumed the plan per handover session-2026-07-27-2 Next Step 1. Executed Phase 3 (Claude Code) and Phase 4 (Hermes) end-to-end via execute-work-package, review-implementation, and same-session review-fix; both reviews closed with no actionable findings remaining. Final gate exit 0, cross-adapter inspection parity confirmed, plan status set to `completed` — plan completion user-confirmed (2026-07-27).

## Progress

### Completed

- Phase 3 (Claude Code) executed via execute-work-package (Blueprint APPROVE-WP3): fresh implementer `ses_05d5d8cb9ffem4Ga9UYiCsyGdF` (prior `ses_05d6a769dffeM9gHs2pemiww70` context-exhausted after evidence gathering, zero edits; secured evidence carried over: hook wire formats, `.mcp.json`/`hooks.json` formats, strict-validate command; an accidental `~/.claude/skills/probe-plugin` scaffold was removed and verified gone).
- Phase 3 delivered: `claude/agent-checkpoint/` plugin (manifest, `.mcp.json`, hooks, MCP runtime/server, hook bridge, statusline sidecar, instruction, README), 17-test suite, installer extension (statusLine-only opt-in settings; base `settings.json`/`~/.claude.json` byte-for-byte), docs. Pinned-CLI smoke 9/9 on claude 2.1.170 (credential-free E2E + plugin discovery without auth).
- Phase 3 primary decision: credential-free proof set accepted (Phase 2 precedent); live model runs blocked by auth in isolated homes — no workaround exists.
- Phase 3 impl review ([reviews/impl-review-phase-3.md](../reviews/impl-review-phase-3.md)): verdict **Accepted**, 0 Critical/0 Major/0 Minor/4 Note — zero actionable findings, no fix loop. update-plan: phase-3 completed; agent-checkpoint-heartbeat phase 5 marked completed.
- Phase 4 (Hermes) pre-execution: pin re-checked (`~/.hermes/hermes-agent` @ `e0b9ab5a` clean — no drift). Stop condition hit on a Phase 1 research gap: `subagent_start` EXISTS on the pinned build (`plugins.py:164` VALID_HOOKS, `tools/delegate_tool.py:1453–1466` fire site, hooks.md docs; `stop` absent). Primary decision: keep the gated session-level design; stop-and-revise of `implementation/phase-4-impl.md` (Stop-and-Revise Log; honesty wording corrected in plan.md/phase-4.md); `subagent_start` documented as available-but-not-adopted (follow-up option).
- Phase 4 executed via execute-work-package (Blueprint APPROVE-WP4): implementer `ses_05d2c5888ffesqKJ6sdSN27AyN` completed Step 1 (`checkpoint-inspect.js` realpath-guard fix + regression test) then context-exhausted; fresh implementer `ses_05d09da42ffeeyEL641mspwWiM` completed Steps 2–8.
- Phase 4 key design decision: installer applies the documented enablement's additive `plugins.enabled` delta as a minimal text edit (because `save_config`/`atomic_yaml_write` rewrites whole files); docs cite `hermes plugins enable|disable` as the user-facing flow; smoke proved `hermes plugins list --json` honors the text-edit enablement.
- Phase 4 delivered: `hermes/agent-checkpoint/` (plugin.yaml, agent_checkpoint.py, __init__.py, README), `hermes/test/test_agent_checkpoint.py` (44 tests), installer Step 9 `install_hermes_checkpoint` (Cursor renumbered 10), docs (`config.yaml.example`, `docs/installation.md`, `docs/agent-checkpoint-heartbeat.md`).
- Phase 4 pinned smoke (v0.19.0, isolated HERMES_HOME): installer exit 0, config diff exactly 3 additive lines, real checkpoint context_used 0.512, checkpoint-inspect read the log, real `~/.hermes` untouched (spool `/tmp/opencode/checkpoint-harness-integration-phase4/`).
- Phase 4 impl review ([reviews/impl-review-phase-4.md](../reviews/impl-review-phase-4.md)): verdict **Needs Rework** (0 Critical/1 Major/3 Minor/2 Note) → same-session review-fix remediated F-1 (disabled-list loud stop), F-2 (negative-branch installer tests), F-3 (inline-dict loud stop), F-4 (telemetry table precision), F-6 (continued-session doc sentence); remediation addendum appended, no actionable findings remain.
- Final state: final gate exit 0 — node 58/58 (0 skipped) + python 44/44 + `bash -n install.sh`; cross-adapter inspection parity confirmed over four-harness logs; all plan DoD items satisfied/annotated; agent-checkpoint-heartbeat phases 4/5 reconciled; plan status → `completed` (user-confirmed 2026-07-27).

### In Progress (interrupted)

- None.

### Not Started (planned but deferred)

- Authenticated live parent/subagent model runs for Claude/Hermes - Reason: credentials unavailable in isolated homes; credential-free proof sets accepted as sufficient (Phase 2 precedent). Optional future verification.
- Hermes `subagent_start` adoption (start-time subagent identity) - Reason: deliberately not adopted (gated session-level scope); documented follow-up option.
- agent-checkpoint-heartbeat phase 6 (PydanticAI adapter) - Reason: out of scope for this plan.

## Key Decisions Made

| Decision | Rationale | Alternatives Considered |
|----------|-----------|------------------------|
| Credential-free proof sets accepted for Phase 3/4 verification (primary) | Live model runs need auth unavailable in isolated homes — no workaround; Phase 2 precedent; contract-parity suites + pinned-CLI smokes prove behavior honestly | Treat as stop gate / find auth workaround (rejected: none exists; disproportionate) |
| `subagent_start` available-but-not-adopted on Hermes (primary) | Gated design is session-level; adopting mid-flight would change gated scope; mirrors the Codex gate-(a) precedent; recorded via stop-and-revise with docs honesty fixes | Adopt it (rejected: scope change); ignore the research gap (rejected: dishonest docs) |
| Installer applies `plugins.enabled` enablement as minimal text edit | `save_config`/`atomic_yaml_write` rewrite whole files, breaking the byte-for-byte NFR; text edit yields exactly 3 additive lines; smoke proved `hermes plugins list --json` honors it | YAML round-trip via `save_config` (rejected: rewrites file); skip enablement (rejected: plugin must be enabled to function) |
| Phase 4 review-fix executed in the same reviewer session | Reviewer retained code, findings, assumptions, and verification context; a fresh implementer would rebuild context and risk a conflicting policy layer | New implementer per finding (rejected: context loss) |

## Current State of Implementation

### Modified Files

| File | Change | Status |
|------|--------|--------|
| `codex/` | New Codex adapter (untracked) | complete, reviewed |
| `claude/` | New Claude Code plugin `claude/agent-checkpoint/` (untracked) | complete, reviewed |
| `hermes/` | New Hermes plugin + test suite (untracked; contains `__pycache__` dirs — clean up/gitignore before any commit) | complete, reviewed |
| `install.sh` | Targets extended: codex profile, claude Step 8, hermes Step 9, Cursor renumbered 10 | complete, reviewed |
| `config.yaml.example`, `docs/installation.md`, `docs/agent-checkpoint-heartbeat.md` | Per-adapter docs, harness matrix, honest telemetry/limits | complete, reviewed |
| `opencode/test/checkpoint-plugin.test.mjs` | Path-canonicalization fix (test bug, Phase 2) | complete, reviewed |
| `packages/checkpoint-core/bin/checkpoint-watch.js` + test | realpath main-guard fix + regression test (Phase 2) | complete, reviewed |
| `packages/checkpoint-core/bin/checkpoint-inspect.js` + test | realpath main-guard fix + regression test (Phase 4 note resolved) | complete, reviewed |
| `plans/checkpoint-harness-integration/` | All plan artifacts incl. 6 reviews, 3 handovers (untracked); plan status `completed` | current |
| `plans/agent-checkpoint-heartbeat/` | Phases 4/5 marked completed (plan.md, phase-4.md, phase-5.md); old impl drafts carry superseded banners | complete |

Nothing committed (git only on explicit user request).

### Pending Tests

- None open. Final gate exit 0: node 58/58 (0 skipped) + python 44/44 + `bash -n install.sh`.

## Blockers & Issues

- None active. Accepted limitation: live model-driven parent/subagent runs for Claude/Hermes require credentials unavailable in isolated homes; credential-free proof sets accepted and documented.

## Next Steps

1. Git commit only on explicit user request — consider `__pycache__` cleanup under `hermes/` (or a gitignore entry) before any commit.
2. Optional: authenticated live parent/subagent runs for Claude/Hermes, if the user wants end-to-end model-driven behavioral proof beyond the accepted credential-free proof sets.
3. Optional follow-up plan candidates: Hermes `subagent_start` adoption (start-time subagent identity); agent-checkpoint-heartbeat phase 6 (PydanticAI adapter, out of scope here).

## Context for Next Session

- Repo root: `/Users/Martin/git/opencode-processing-skills`, branch `feature/agent-checkpoint-heartbeat`. Nothing committed; `codex/`, `claude/`, `hermes/`, `plans/checkpoint-harness-integration/` untracked. Git only on explicit user request.
- Plan `checkpoint-harness-integration` is **completed** (user-confirmed 2026-07-27); no phase work remains. Reopen only for the optional follow-ups above.
- Pinned builds: codex-cli 0.131.0, claude 2.1.170, Hermes v0.19.0 (upstream `e0b9ab5a`).
- Evidence spools (machine-local): `/tmp/opencode/checkpoint-harness-integration-phase3/`, `/tmp/opencode/checkpoint-harness-integration-phase4/` (plus phase1/phase2 spools from earlier sessions).
- Hermes pinned-build facts: `subagent_start` exists (`plugins.py:164`, fired from `tools/delegate_tool.py:1453–1466` with parent/child identity payloads, documented) but is not adopted; `stop` is absent; `save_config`/`atomic_yaml_write` rewrite whole files (hence the text-edit enablement); `hermes plugins enable|disable` is the documented user-facing flow.
- Prior implementer sessions are context-exhausted; always start fresh implementer sessions per work package (handover digests carry state).
